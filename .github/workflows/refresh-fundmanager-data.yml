name: Refresh fundmanager static data

on:
  workflow_dispatch:
  # Disabled 2026-04-26: the scheduled generator was repeatedly publishing
  # internal/private workspace data to public GitHub Pages /docs/data paths.
  # Re-enable only after the generator is scrubbed and outputs are safe for public Pages.
  # schedule:
  #   - cron: '*/15 * * * *'

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Static data publishing disabled
        run: |
          echo "Fundmanager static-data publishing is disabled because it exposed private/internal data on eb28.co."
          echo "Scrub the generator and choose an intentionally public output contract before re-enabling."
